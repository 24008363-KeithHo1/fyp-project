<%- include('../partials/header') -%>
<%- include('../partials/navbar') -%>
<%- include('./_admin_nav') -%>

<section class="admin-page">
  <h2>Invite Tokens</h2>
  <p class="text-muted">Outstanding invite tokens and their status</p>

  <% if (!tokens || tokens.length === 0) { %>
    <div class="alert alert-info">No invite tokens found.</div>
  <% } else { %>
    <table class="table table-striped">
      <thead>
        <tr>
          <th>ID</th>
          <th>Email</th>
          <th>Inviter</th>
          <th>Expires</th>
          <th>Used</th>
        </tr>
      </thead>
      <tbody>
        <% tokens.forEach(function(t){ %>
          <tr>
            <td><%= t.id %></td>
            <td><%= t.email %></td>
            <td><%= t.inviter ? t.inviter.name : (t.inviterId || '') %></td>
            <td><%= t.expiresAt ? new Date(t.expiresAt).toISOString().slice(0,19).replace('T',' ') : '' %></td>
            <td><%= t.used ? 'Yes' : 'No' %></td>
          </tr>
        <% }) %>
      </tbody>
    </table>
  <% } %>
</section>

<%- include('../partials/footer') -%>
